<?php
/* ==========================================================================
   config.sample.php — نمونه‌ی تنظیمات سرور

   ── چه کار کنم؟ ───────────────────────────────────────────────────────
   ۱. یک کپی از این فایل بگیرید و اسمش را بگذارید  config.php
   ۲. مقدارهای خالی را پر کنید
   ۳. تمام
   ──────────────────────────────────────────────────────────────────────

   ⚠️ فایل config.php رمز دیتابیس و پنل پیامک را دارد.
      • داخل گیت نمی‌رود (در .gitignore هست)
      • از طریق وب هم قابل خواندن نیست (در api/.htaccess بسته شده)
      • هیچ‌وقت محتوایش را در ایمیل، چت یا تیکت پشتیبانی نفرستید

   این فایلِ نمونه باید همیشه خالی بماند.
   ========================================================================== */

return [

    /* ================= پایگاه داده ================= */
    /* از cPanel → MySQL Databases. نام دیتابیس و کاربر معمولاً پیشوند
       حساب هاست را دارند، مثل zohrezar_database */
    'db' => [
        'host'    => 'localhost',
        'name'    => '',
        'user'    => '',
        'pass'    => '',
        'charset' => 'utf8mb4',
    ],

    /* ================= پنل پیامک ================= */
    'sms' => [
        /* تا وقتی false باشد هیچ پیامکی فرستاده نمی‌شود و کد تأیید
           روی صفحه نشان داده می‌شود. برای تست اولیه عالی است؛ روی
           سایت واقعی حتماً true. */
        'enabled'  => false,

        'provider' => 'niazpardaz',

        'username' => '',   // نام کاربری پنل
        'password' => '',   // رمز عبور پنل
        'api_key'  => '',   // کلید API (اگر با رمز کار نکرد)

        /* کدام یک در جای رمز به سامانه برود:
             'username_password' → مقدار password  (مستندشده، پیش‌فرض)
             'api_key'           → مقدار api_key
           اگر یکی جواب نداد، فقط همین سطر را عوض کنید. */
        'auth_mode' => 'username_password',

        /* شماره‌ی خط فرستنده — از پنل، بخش «خطوط من».
           برای کد تأیید باید خط خدماتی باشد، نه تبلیغاتی. */
        'from'     => '',

        'base_url' => 'http://in.payamak-service.ir/api/v2/RestWebApi/',
        'timeout'  => 15,

        /* ── قالب‌های پیامک ──────────────────────────────────────────
           نیازپرداز متد جداگانه‌ای برای «پترن» ندارد. متنِ کامل با متد
           معمولی می‌رود و سامانه آن را با الگوهای تأییدشده‌ی حساب شما
           تطبیق می‌دهد. اگر با هیچ الگویی نخواند، خطای ۱۸ می‌دهد.

           پس متن این‌جا باید کاراکتر‌به‌کاراکتر همان چیزی باشد که در
           پنل تأیید شده — حتی فاصله‌ها و دو‌نقطه.

           هر قالبی که null باشد، آن نوع پیامک اصلاً فرستاده نمی‌شود.
           این عمدی است: تا وقتی الگویی در پنل تأیید نشده، فرستادنش
           فقط خطا می‌دهد و اعتبار می‌سوزاند.
           ───────────────────────────────────────────────────────────── */
        'templates' => [
            /* الگوی تأییدشده: کد تایید شما: {کد} */
            'otp'      => 'کد تایید شما: {code}',

            /* این پنج تا تا وقتی الگویشان در پنل تأیید نشده باید
               null بمانند. قالبِ null اصلاً فرستاده نمی‌شود — پس نه
               خطای ۱۸ می‌گیرید و نه اعتبار می‌سوزد.

               متغیرهای همه‌ی قالب‌ها:  {date}  {time}  {service}
                 {date}    مثل: چهارشنبه ۲۵ شهریور
                 {time}    مثل: ۱۶:۳۰
                 {service} مثل: لیفت و لمینت مژه و ابرو

               متغیرهای اضافه‌ی هر قالب:
                 rejected     → {reason}  علتی که در پنل مدیریت نوشته‌اید
                 admin_alert  → {name} {phone}  مشخصات مشتری

               admin_alert به شماره‌های admin_phones می‌رود، نه به مشتری. */
            'approved'    => null,
            'rejected'    => null,
            'reminder'    => null,
            'received'    => null,
            'admin_alert' => null,
        ],
    ],

    /* ================= کد ورود ================= */
    'otp' => [
        'length'       => 4,    // تعداد رقم کد
        'ttl_seconds'  => 120,  // اعتبار کد
        'resend_after' => 60,   // فاصله‌ی لازم بین دو درخواست
        'max_attempts' => 5,    // تعداد تلاش نادرست مجاز
        'per_hour'     => 5,    // سقف درخواست کد برای هر شماره
        'per_ip_hour'  => 20,   // سقف درخواست کد از هر دستگاه
    ],

    /* ================= رزرو نوبت ================= */
    'booking' => [
        'days_ahead'          => 14,  // تقویم چند روز جلو را نشان دهد
        'cancel_window_hours' => 24,  // لغو نوبت قطعی تا چند ساعت قبل
        'max_open_requests'   => 2,   // سقف درخواست در انتظار هر نفر
        'closed_weekday'      => 5,   // تعطیلی هفتگی (۵ = جمعه، ۰ = یکشنبه)

        /* ساعت‌های کاری. باید با assets/js/data/appointments.js یکی
           باشد تا نسخه‌ی آفلاین سایت هم همین‌ها را نشان دهد. */
        'times' => ['10:00', '11:30', '13:00', '14:30', '16:00', '17:30', '19:00'],
    ],

    /* ================= مدیران ================= */
    /* هر کس با این شماره‌ها وارد شود، خودکار نقش مدیر می‌گیرد و به
       /panel.html دسترسی پیدا می‌کند. شماره‌ای که این‌جا نباشد، حتی
       اگر قبلاً مدیر بوده، به کاربر عادی برمی‌گردد. */
    'admin_phones' => [
        '09178399055',
        '09961217945',
    ],

    /* ================= عمومی ================= */
    'app' => [
        'site_url' => 'https://zohrezare.ir',
        'timezone' => 'Asia/Tehran',

        /* فقط موقع عیب‌یابی true کنید. وقتی روشن باشد، متن خطای فنی
           در پاسخ API می‌آید — که نباید دست کاربر بیفتد. */
        'debug'    => false,
    ],
];
